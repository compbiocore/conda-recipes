about:
  home: http://metacpan.org/pod/AutoLoader
  license: perl_5
  summary: load subroutines only on demand
build:
  number: 0
package:
  name: perl-autoloader
  version: 5.74
requirements:
  build:
  - perl
  run:
  - perl
test:
  commands:
  - perl -e "use AutoLoader"
