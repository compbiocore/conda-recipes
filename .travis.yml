language: generic

os:
  -linux

sudo: required
services:
  - docker

# set up docker image
before_install:
  - docker pull mhowison/conda-build:v2
  - docker run -v conda-recipes:/home/conda/recipes --name cbc -a stdin -a stdout -a stderr mhowison/conda-build:v2 /bin/bash -c "sudo yum -y install git; cd /home/conda/recipes; pwd;  ls -ltrh ; echo 'directory listing'; python /home/conda/recipes/build.py /home/conda/recipes compbiocore"
  - docker ps -a
  #- docker exec cbc sudo yum -y install git
  #- docker exec cbc python /home/conda/recipes/build.py /home/conda/recipes compbiocore
  #- docker start 
